# Stray test artifacts from earlier sandbox probing
.write_test
index_test.html
*.aside
*.dead

# macOS
.DS_Store

# Backups from normalize_study2_audio.sh — 17 MB of pre-normalize originals
# (regenerable from ElevenLabs; no need to commit)
study2/tmp/

# R session state — huge and fully regenerable (never commit)
scripts/.RData
scripts/.Rhistory
.Rproj.user/

# Fitted Bayesian model caches (regenerated by the analysis scripts)
scripts/fits/
scripts/results/**/models/
*.rds

# Study 3 audio-normalization backups (same as study2/tmp)
study3/tmp/
